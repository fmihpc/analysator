How to use:

Make sure you have MayaVi 4.3.0 installed (ubuntu: sudo apt-get install mayavi2 in terminal). NOTE: VERSION 4.0.0 OF MAYAVI DOES *NOT* WORK

Make sure you have ipython installed (ubuntu: sudo apt-get install ipython)

Start ipython and import pytools or some module from it with the command:

import pytools

To get more help on commands you can type for example pytools.vlsvreader.VlsvFile? and hit enter.

